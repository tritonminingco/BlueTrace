# Database
POSTGRES_DSN=postgresql://bluetrace:bluetrace@localhost:5432/bluetrace

# Redis
REDIS_URL=redis://localhost:6379/0

# API
API_BASE_URL=http://localhost:8080
CORS_ORIGINS=["http://localhost:3000","http://localhost:8080"]
LOG_LEVEL=INFO

# Stripe
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret
STRIPE_PRODUCT_FREE=prod_free_id
STRIPE_PRODUCT_PRO=prod_pro_id
STRIPE_PRODUCT_ENTERPRISE=prod_enterprise_id

# Rate Limits (JSON)
RATE_LIMITS_JSON={"free": {"requests": 30, "window": 60}, "pro": {"requests": 300, "window": 60}, "enterprise": {"requests": 10000, "window": 60}}

# Admin
ADMIN_SEED_EMAIL=admin@bluetrace.dev

# Security
SECRET_KEY=change-this-to-a-random-secret-key-in-production
API_KEY_SALT=change-this-salt-for-api-key-hashing

# Telemetry
OTEL_SERVICE_NAME=bluetrace-api
OTEL_EXPORTER_TYPE=console

# Workers
DRAMATIQ_BROKER=redis
DRAMATIQ_THREADS=8

